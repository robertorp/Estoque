
namespace Estoque.Estoque.TestesUnitarios
{
    public class CadastrarProdutoTestes
    {
        /*private readonly Faker _faker;

        private AutoMocker _mocker;
        private CadastrarProdutoCommandHandler _cadastrarProdutoCommandHandler;

        public CadastrarProdutoTestes()
        {
            _faker = new Faker("pt_BR");
            _mocker = new AutoMocker();
            _cadastrarProdutoCommandHandler = _mocker.CreateInstance<CadastrarProdutoCommandHandler>();
        }

        [Fact]
        public async void Produto_Cadastrar_DeveTerSucesso()
        {
            // Arrange
            var command = new CadastrarProdutoCommand
            {
                Nome = _faker.Commerce.ProductName(),
                UnidadeMedida = _faker.Commerce.ProductUnitMeasure(),
                Preco = decimal.Parse(_faker.Commerce.Price(0.10m, 9.999m)),
            };

            
            // Action
            var resposta = await _cadastrarProdutoCommandHandler.Handle(command, CancellationToken.None);


            // Assert
            Assert.True(resposta.Success);
        }*/
    }
}